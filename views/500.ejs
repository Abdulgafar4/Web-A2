<!DOCTYPE html>
<html lang="en" data-theme="dim">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>500 - Server Error</title>
    <link href="/css/main.css" rel="stylesheet">
</head>
<body class="min-h-screen bg-base-100">
    <%- include('partials/navbar', {page: '' }) %>

    <div class="container mx-auto px-4 mt-24 text-center">
        <div class="hero min-h-[200px] bg-base-200 rounded-box my-8">
            <div class="hero-content text-center">
                <div class="max-w-md">
                    <h1 class="text-7xl font-bold text-red-500 mb-6">500</h1>
                    <p class="text-2xl mb-8">Server Error</p>
                    <p class="text-lg mb-8"><%= message %></p>
                    <a href="/" class="btn btn-primary px-6 py-3">Go Back Home</a>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-gray-800 text-white py-8 mt-auto">
        <div class="container mx-auto px-4 text-center">
            <p>Student Name: <%= studentName %> | Student ID: <%= studentId %></p>
            <p>Last Updated: <%= timestamp.toLocaleString() %></p>
        </div>
    </footer>
</body>
</html>